# План пере-расселения UI (реорг компоновки редактора)

Цель: разложить **существующий** функционал по правильным зонам/экранам.
Без новых фич. Бэкенд (`server/src/routes/*`) не трогаем — это чисто клиентское пере-расселение.

## Принцип
Каждая зона экрана = одна работа, и она там всегда:
- **Лево** — структура (главы), сворачивается.
- **Центр** — письмо.
- **Право — инспектор** (вкладки), сворачивается + разворот на весь экран. Дом для Библии/Перо/Истории/Статистики.
- **Низ — рабочая панель**: Диктовка + переключатели вкладок инспектора + Фокус.
- **Верх** — тонкое форматирование + Поиск/Экспорт/шрифт/ширина + аватар (Настройки).

## Веса функционала (согласовано)
- **Герой:** D — авто-библия (Мир), E — противоречия/сверка.
- **Отличия:** B — диктовка, F — спроси Перо (аналитик), I — импорт/экспорт.
- **База:** A письмо, C организация, G история, H прогресс, J аккаунт.
- ИИ = «библиотекарь», питающий D+E+F. Генерацию текста не выносим в функционал.

## Карта переезда (компонент → новый дом)
| Существующее | Новый дом | Тип |
|---|---|---|
| `StoryBible.tsx` (страница) + `StoryBiblePanel.tsx` + Reference-срез | вкладка **«Мир»** (общий `BibleView` + переключатель глава/проект + инбокс) | слияние 3→1 |
| `CoauthorPanel.tsx` | вкладка **«Перо»**; убрать генерацию, оставить аналитику | перенос+чистка |
| Противоречия (скан/часть библии) | вкладка/срез **«Противоречия»** + бейдж | перенос |
| `ProjectSyncPanel.tsx` | баннер «N глав изменились — обновить» в Мир/Противоречия + бейдж | слияние |
| `RevisionPanel.tsx` | вкладка **«История»** | перенос as-is |
| `WritingStatsPanel.tsx` | вкладка **«Статистика»** (+ тихий счётчик внизу) | перенос as-is |
| `BottomToolbar.tsx` | рабочая нижняя панель: кнопки переключают вкладки инспектора | перенос+чистка |
| `AppSidebar/ChapterSidebar` | левый рельс = только главы; «‹ К проектам» в шапке; Настройки → аватар | чистка |
| `InlineBubbleMenu.tsx` | убрать ИИ-трансформации, оставить форматирование | чистка |
| `EditorCanvas` (верх) | форматирование тонко + Поиск + Экспорт + шрифт/ширина + аватар | перенос |
| Dashboard/Settings/Onboarding/Login/Landing/Privacy/Demo | без изменений | — |
| `IdeaLibrary.tsx` | заморозить (скрыть вход) | отложено |

Новый шелл (не фича): `RightInspector` (таб-бар + сворачивание/разворот).
Рефактор состояния `Editor.tsx`: 6 `isXOpen` → `inspectorTab` + `inspectorOpen/Expanded`.

Без изменений: все хуки, расширения Tiptap, весь бэкенд.

## Шаги (от низкого риска)
- [x] **1. Левый рельс:** убрать дубль «Проекты» (= стрелка-назад). ✓ Готово
- [x] 2. ✓ Удовлетворено существующей архитектурой: панели уже живут в одном правом `<aside>` (взаимоисключающие), нижняя панель их переключает. Отдельный верхний таб-бар не добавляем — он дублировал бы нижний «пульт». Внутренний рефактор 6 булевых → один `inspectorTab` отложен (невидимый, риск без пользы).
- [~] 3. Дедуп Библии ✓ (ядро): в редакторе «Библия истории» в левом рельсе открывает встроенный инспектор (`StoryBiblePanel`), а не уводит на `/bible` — не выходишь из письма. *(Осталось: полностью слить страницу StoryBible + Reference в общий `BibleView` и удалить маршрут `/bible` — крупный рефактор, отдельно.)*
- [~] 4. Бейджи на героя: ✓ бейдж «N на одобрение» на кнопке «Библия» (pending сущности + правки). Синхр-бейдж уже был. *(Свёртка Синхронизации в баннер — со step 3, когда сложится «Мир».)*
- [x] 5. Чистка ИИ-генерации ✓: bubble-menu (InlineBubbleMenu) убран из редактора; Coauthor — оставлены аналитические действия (Суммируй/Противоречия/Что изменилось/Извлечь), генерация убрана. *(InlineBubbleMenu.tsx осиротел — удалить при чистке.)*
- [~] 6. Сворачивание + чистый центр ✓: левый рельс сворачивается (кнопка «Скрыть» + кнопка «Показать главы» в тулбаре); правый инспектор открывается/закрывается; чистый центр = режим «Фокус». *(Осталось: Настройки→аватар — завязано на полное удаление страниц Bible/Ideas из step 3; разворот инспектора на весь экран — отдельно.)*

Макет-референс: `editor-layout-mockup.html`.

---

## Фаза 2 — понятное ядро «Мир» (решено в обсуждении механики)

Ядро одной фразой: **«Пиши → Перо читает рукопись → подтверди находки → Перо ловит нестыковки.»**

Проблема сейчас: цикл размазан по 4 кнопкам-загадкам (Библия/Справочник/Ревизия/
Синхронизация), извлечение запускается из 4 мест и оно ручное (обещание «Перо само
читает» не выполняется).

Решение:
- **Одна поверхность «Мир»** = весь цикл: статус-чтение · **Новое** (инбокс ✓/✕) ·
  **Нестыковки** · подтверждённый мир по типам (клик по сущности → «где встречается» +
  «арка» — бывшая «Ревизия», встроена).
- **Полуавто-чтение (решено):** Перо замечает изменённые/новые главы и показывает в
  статусе «Мира» кнопку **«Прочитать»** (нудит, читает по одному клику; квота под
  контролем). Детектор устаревших глав (`lastExtractedAt`) уже есть — переподать как
  статус-строку, а не иконку «Синхронизация».
- **4 кнопки → одна «Мир» (N).** Нижняя панель: `Диктовка ǀ Перо · Мир(N) · Статистика ǀ Фокус`.
- Перо-чат остаётся «спроси про мир»; его дубли (Извлечь/Противоречия) — ярлыки в «Мир».

Шаги фазы 2 (от низкого риска):
- [~] 2.1/2.3 ✓ Статус-строка чтения вынесена в шапку «Библии»: всегда видно
      «Перо читает…/прочитало/Глава изменилась — Прочитать» (полуавто-нудж, один клик).
      Дубль-баннер из вкладки «Новое» убран. *(Осталось: вкладка «Нестыковки» как
      отдельная + финальный порядок вкладок.)*
- [x] 2.3 ✓ (см. 2.1/2.3 выше — статус «Прочитать» в шапке).
- [~] 2.4 ✓ часть: иконки из «мира писателя» — «Ревизия» git→телескоп («Поиск по миру:
      найти сущность, арка»), «Синхронизация» пульс→refresh. *(Полный перенос trace/арки
      внутрь карточки сущности — отдельно.)*
- [ ] 2.2 Свести «Справочник» (срез главы) в «Мир» переключателем глава/проект. **Нужно:
      построить scope-фильтр в StoryBiblePanel; затем убрать кнопку «Справочник».**
- [ ] 2.5 Нижняя панель: 4 кнопки → одна «Мир»(N). **Нужно решение: имя «Мир» vs «Библия
      истории» (установившийся термин), и куда сворачиваются Справочник/Синхронизация/
      Ревизия, чтобы не потерять функции.** Делать интерактивно, не автономно.

### Статус фазы 2
Готово в коде и проверено: статус-цикл «Прочитать» в шапке «Библии» (полуавто), убран
дубль-баннер, писательские иконки. Осталось (требует продуктовых решений + фолдинга
панелей, делать с автором): полное слияние 4→1 «Мир», scope-фильтр Справочника,
trace/арка внутрь карточки, отдельная вкладка «Нестыковки».

---

## Фаза 3 — «Мир» как набор интерактивных линз (решено 16.06.2026)

Ключевая поправка: «Мир» — **не плоский каталог сущностей**, а **набор живых срезов**
одной рукописи. Конкуренты делают виды руками; наш ров — линзы **авто из прозы**,
**кликабельны в текст** (узел/точка → сцена), **подсвечивают нестыковки на самом виде**.

Объекты (фундамент, остаются): Персонажи · Локации · Предметы/артефакты · Правила мира
(+ значимость, атрибуты, «впервые: гл.N»), Связи, События/арки, Находки, Правки, Нестыковки.

Линзы (виды над теми же объектами):
- [ ] **Присутствие** (сетка сущность×глава: кто в какой главе, темп) — **v1 флагман**,
      данные готовы (trace + «впервые: гл.N»), рисуется просто, лечит «потерял нить в длинной книге».
- [ ] **Связи** (граф: узлы-сущности по типам, рёбра-`entity_links`) — **v1**, нужен граф-рендер.
- [ ] **Таймлайн** (события по главам) — v2, данные `entity_events`.
- [ ] **Арки** (линия персонажа через книгу) — v2.
- [ ] **Карта** (локации в пространстве) — позже, нужен слой координат.
- [x] **Каталог** (карточки, правка/одобрение) — уже есть, одна из линз.

Старт: **Присутствие → Связи**. Каждая линза: авто-построение + клик в текст + нестыковки на виде.

---

## Фаза 4 — нативный экран: текст-дом + пульс-рельс + линзы-оверлей (решено 16.06.2026)

Проблема: управление «Миром» = лоток из ~8 кнопок-загадок, нет рабочего процесса, нет
деления «всегда видно» vs «по требованию». Не нативно — «ищи кнопку», а не «чувствуй вызов».

Каркас (3 слоя):
- **Холст текста (дом)** — минимум хрома.
- **Пульс мира** — тонкий рельс справа, **всегда виден**: точка статуса чтения
  (зелёная «прочитано» / янтарная «Прочитать»→читать главу), Находки (✦ N → оверлей
  «Новое»), Нестыковки (⚠ N → оверлей), ниже вход «Мир» (лончер линз) и «Перо» (чат).
- **Линзы по требованию = оверлей** (наезжает справа, Esc/клик-вне → закрыть). Вызов:
  рельс · **⌘K / «/»** (намерение словом) · контекст (выделил имя → «Показать в Мире /
  Где встречается»). Клик по узлу/строке → прыжок в текст. Нестыковки подсвечены на виде.

Рабочий процесс: **пиши → глянул на пульс → по требованию загляни → вернись к письму.**
Низ → Диктовка + тихий счётчик слов. Фокус = скрыть рельс и низ.

Решение по развилке (16.06): **тонкий рельс + оверлей** (а не пульс-в-шапке/правый
инспектор и не «только по требованию») — максимально «вызвал → посмотрел → вернулся».

Шаги (от низкого риска, делать с автором):
- [x] 4.1 ✓ Рельс «Пульс мира» (`PulseRail.tsx`): тонкая всегда-видимая полоса справа
      (скрыта в фокусе и на <md). Статус чтения (глаз «Прочитать»/спиннер/галка),
      Находки (Bell+N→инбокс), Нестыковки (⚠+N→Справочник), Мир (→линзы), Перо (→чат).
      Панель открывается слева от рельса, рельс остаётся на краю. Проверено в превью.
      *(Нижняя панель пока не тронута — её слим в 4.6.)*
- [ ] 4.2 Лончер «Мир» в рельсе → список линз (Каталог есть; Присутствие/Связи — Фаза 3).
- [x] 4.3 ✓ Инспектор «Мира» — оверлей: наезжает справа поверх текста (слева от рельса),
      письмо не раздвигается; закрытие Esc/крестик/затемнение. (Клик-в-текст из линз — в 4.2.)
- [ ] 4.4 ⌘K / «/» командная строка (намерение словом) — унифицированный вызов.
- [ ] 4.5 Контекстное мини-меню на выделении имени.
- [x] 4.6 ✓ Низ ужат до Диктовки + Фокуса; всё прочее (Перо/Мир/Справочник/Поиск
      по миру/Статистика) переехало в пульс-рельс. Синхронизация свёрнута (per-chapter
      «Прочитать» на рельсе; bulk «прочитать все» переедет в шапку «Мира» в шаге 4.2).
