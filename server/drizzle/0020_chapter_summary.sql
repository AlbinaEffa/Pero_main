-- Краткая аннотация главы (1–2 предложения «что произошло»), извлекается ИИ
-- в том же проходе. Двойное назначение: человеку — оглавление-синопсис,
-- ИИ — дешёвый контекст вместо полной главы.
ALTER TABLE chapters ADD COLUMN IF NOT EXISTS summary text;
